구간이 주어졌을 때 최소치 하나만을 찾는 게 아니라 가장 작은 두 개의 값을 찾는 문제를 푸는 구간트리를 만들어보자

구간 트리를 사용하려면 두 개의 작은 부분 구간에 대해 계산한 값을 효율적으로 합치는 방법이 있어야 한다.
두 개의 자식 구간에 대해 각각 최소치 두 개씩을 찾았다고 하자.
이들을 어떻게 합쳐서 전체 구간에 대한 답을 계산할 수 있을까?
-> 네 개의 값 중에서 가장 작은 두 개를 선택하면 된다.
구간의 길이가 1인 경우에는 해당 구간에 포함된 숫자와 아주 큰 값INF를 반환하면 된다.