#include <iostream>
#include <vector>
#include <queue>
#include <algorithm>
#include <cstring>
#define fasti ios_base::sync_with_stdio(false); cin.tie(0);
#define fastio ios_base::sync_with_stdio(false); cin.tie(0); cout.tie(0);
#define INF 1e9+7
#define NUM 100001
#define pii pair<int, int>

typedef long long ll;
// typedef pair<int, int> pii;

using namespace std;

int N, M;

ll tree[NUM * 4];

void input(){
    cin >> N >> M;
}

ll make_tree(int startidx, int endidx, int node){
    if(startidx == endidx) return tree[]
}

void solve(){
    int o, s, t;
    for(int i = 0; i < M; i++){
        cin >> o >> s >> t;
        if(o == 0)
    }
}

int main(){
    input();
    solve();
    
    return 0;
}
